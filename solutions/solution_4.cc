#include "solutions.h"
/** 
 * 문제 4: 지역 별 공급자 잔고 분포 분석
 * 지역 별 공급자의 잔고 분포를 분석하려고 합니다.
 * 지역 별 공급자 잔고의 분포가 편중되어 있는지 확인하기 위해 
 * 지역 별 공급자 잔고의 비대칭도(skewness)를 구하여 제출해주세요.
 * 아래 나열된 순서대로 제출 부탁드립니다.
 * (1) 지역 ID
 * (2) 왜도: 3 * (average - median) / std_dev
 *     - average : 평균값
 *     - median : 중간값
 *     - std_dev : 표준편차
 */
Table SolutionForProblem4(const Table &nation, const Table &supplier) {
  Table result;

  /* FILL IN */

  return result;
}