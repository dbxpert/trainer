#include "solutions.h"
/** 
 * 문제 1: 제품 별 판매 통계 분석
 * 주문이 들어온 제품에 대해 아래 값을 집계하여 제출해주세요.
 * 집계 값은 아래 나열된 순서대로 제출 부탁드립니다.
 * (1) 주문 수량 총계: sum(quantity)
 * (2) 구매 가격 총계: sum(extended_price)
 * (3) 할인율을 적용한 구매 가격 총계: sum(extended_price * (1 - discount))
 * (4) 할인율과 부가세를 적용한 구매 가격 총계: sum(extended_price * (1 - discount) * (1 + tax))
 * (5) 평균 주문 수량: avg(quantity)
 * (6) 평균 구매 가격: avg(extended_price)
 * (7) 평균 할인율: avg(discount)
 * (8) 주문량 총계: count(*)
 */
Table SolutionForProblem1(const Table &lineitem) {
  Table result;

  /* FILL IN */

  return result;
}